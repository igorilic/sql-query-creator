# SQL Query Creator — Implementation Plan

**Spec:** `feature-20260320-120720-spec.md`
**Pipeline:** github-feature (feature-20260320-120720)

---

## Steps

### Step 1: Monorepo scaffold with pnpm workspaces
- **Test**: Verify `pnpm install` succeeds, `pnpm --filter web build` produces a working Next.js build, and `packages/ui` is resolvable from `apps/web`.
- **Implement**: Create `pnpm-workspace.yaml`, root `package.json`, `apps/web/` (Next.js 16 app with TypeScript + Tailwind CSS v4), `packages/ui/package.json`. Copy Catalyst TypeScript components from `catalyst-ui-kit/typescript/` into `packages/ui/src/`. Configure `tsconfig.json` paths so `@ui/*` resolves to `packages/ui/src/*`.
- **Files**: `pnpm-workspace.yaml`, `package.json`, `apps/web/package.json`, `apps/web/tsconfig.json`, `apps/web/next.config.ts`, `apps/web/postcss.config.mjs`, `apps/web/src/app/layout.tsx`, `apps/web/src/app/page.tsx`, `apps/web/src/app/globals.css`, `packages/ui/package.json`, `packages/ui/src/*.tsx`
- **Commit**: `chore(monorepo): scaffold pnpm workspace with Next.js app and Catalyst UI package`

---

### Step 2: Shared TypeScript types and schema models
- **Test**: Unit tests for type guards — `isPostgresConfig(config)` returns true for valid PG config, false for SQLite config. `isValidSchema(schema)` validates schema shape. `parseSqlFromMarkdown(text)` extracts SQL from markdown code blocks.
- **Implement**: Create `packages/shared/` with `types.ts` (DatabaseSchema, TableInfo, ColumnInfo, ChatMessage, ConnectionConfig, ConnectionStatus), type guard functions, and `parseSql.ts` utility.
- **Files**: `packages/shared/package.json`, `packages/shared/src/types.ts`, `packages/shared/src/type-guards.ts`, `packages/shared/src/parse-sql.ts`, `packages/shared/src/__tests__/type-guards.test.ts`, `packages/shared/src/__tests__/parse-sql.test.ts`
- **Commit**: `test(shared): type guards and SQL parser` then `feat(shared): add shared types, type guards, and SQL parser`

---

### Step 3: Database connection service — PostgreSQL
- **Test**: Unit tests with mocked `pg` client — `connectPostgres(config)` returns a client on success, throws typed error on invalid credentials. `disconnectPostgres(client)` calls `end()`.
- **Implement**: Create `apps/web/src/lib/db/postgres.ts` with connect/disconnect functions. Create `apps/web/src/lib/db/types.ts` for DatabaseClient interface.
- **Files**: `apps/web/src/lib/db/postgres.ts`, `apps/web/src/lib/db/types.ts`, `apps/web/src/lib/db/__tests__/postgres.test.ts`
- **Commit**: `test(db): PostgreSQL connection service` then `feat(db): PostgreSQL connection with typed errors`

---

### Step 4: Database connection service — SQLite
- **Test**: Unit tests with mocked `better-sqlite3` — `connectSqlite(config)` returns a client for a valid file path, throws for nonexistent path. `disconnectSqlite(client)` calls `close()`.
- **Implement**: Create `apps/web/src/lib/db/sqlite.ts` following same DatabaseClient interface.
- **Files**: `apps/web/src/lib/db/sqlite.ts`, `apps/web/src/lib/db/__tests__/sqlite.test.ts`
- **Commit**: `test(db): SQLite connection service` then `feat(db): SQLite connection with typed errors`

---

### Step 5: Unified connection manager
- **Test**: `ConnectionManager.connect(pgConfig)` delegates to postgres service. `ConnectionManager.connect(sqliteConfig)` delegates to sqlite service. `ConnectionManager.connect()` when already connected closes previous connection first. `ConnectionManager.getStatus()` returns correct ConnectionStatus.
- **Implement**: Create `apps/web/src/lib/db/connection-manager.ts` — singleton that manages the active connection, tracks status, and provides a unified interface.
- **Files**: `apps/web/src/lib/db/connection-manager.ts`, `apps/web/src/lib/db/__tests__/connection-manager.test.ts`
- **Commit**: `test(db): unified connection manager` then `feat(db): connection manager with single-connection enforcement`

---

### Step 6: Schema introspection — PostgreSQL
- **Test**: Given a mocked PG client returning `information_schema` query results, `introspectPostgres(client)` returns a `DatabaseSchema` with correct tables, columns, types, PKs, and FKs. Test empty database returns `{ tables: [], dialect: 'postgresql' }`.
- **Implement**: Create `apps/web/src/lib/db/introspect-postgres.ts` — queries `information_schema.columns`, `information_schema.table_constraints`, and `information_schema.key_column_usage`.
- **Files**: `apps/web/src/lib/db/introspect-postgres.ts`, `apps/web/src/lib/db/__tests__/introspect-postgres.test.ts`
- **Commit**: `test(db): PostgreSQL schema introspection` then `feat(db): introspect PostgreSQL schema with FK resolution`

---

### Step 7: Schema introspection — SQLite
- **Test**: Given a mocked SQLite client returning `sqlite_master` / `PRAGMA table_info` / `PRAGMA foreign_key_list` results, `introspectSqlite(client)` returns correct `DatabaseSchema`. Test empty database returns `{ tables: [], dialect: 'sqlite' }`.
- **Implement**: Create `apps/web/src/lib/db/introspect-sqlite.ts`.
- **Files**: `apps/web/src/lib/db/introspect-sqlite.ts`, `apps/web/src/lib/db/__tests__/introspect-sqlite.test.ts`
- **Commit**: `test(db): SQLite schema introspection` then `feat(db): introspect SQLite schema with FK resolution`

---

### Step 8: LM Studio client with streaming
- **Test**: Mock `fetch` — `sendChatCompletion(messages, options)` sends correct payload to `/v1/chat/completions` with `stream: true`. Test it yields streamed token chunks. Test connection error returns typed `LmStudioError`. Test timeout after 30s.
- **Implement**: Create `apps/web/src/lib/llm/lm-studio-client.ts` — reads `LMSTUDIO_BASE_URL` env var (default `http://localhost:1234/v1`), sends chat completions, returns async iterable of token chunks.
- **Files**: `apps/web/src/lib/llm/lm-studio-client.ts`, `apps/web/src/lib/llm/types.ts`, `apps/web/src/lib/llm/__tests__/lm-studio-client.test.ts`
- **Commit**: `test(llm): LM Studio streaming client` then `feat(llm): OpenAI-compatible streaming client for LM Studio`

---

### Step 9: Prompt builder
- **Test**: `buildSystemPrompt('postgresql', schema)` includes dialect and formatted schema. `buildSystemPrompt('postgresql', null)` omits schema section. `buildMessages(history, userMessage, schema, dialect)` returns correctly ordered message array with system prompt, conversation history, and new user message. History is trimmed to 20 messages.
- **Implement**: Create `apps/web/src/lib/llm/prompt-builder.ts`.
- **Files**: `apps/web/src/lib/llm/prompt-builder.ts`, `apps/web/src/lib/llm/__tests__/prompt-builder.test.ts`
- **Commit**: `test(llm): prompt builder with schema context` then `feat(llm): prompt builder with dialect-aware schema formatting`

---

### Step 10: API route — POST /api/connect
- **Test**: Mock connection manager. POST with valid PG config returns `{ status: 'connected', type: 'postgresql' }`. POST with invalid config returns 400. POST when connection fails returns `{ status: 'error', error: '...' }`.
- **Implement**: Create `apps/web/src/app/api/connect/route.ts` — validates body, calls ConnectionManager, returns status.
- **Files**: `apps/web/src/app/api/connect/route.ts`, `apps/web/src/app/api/connect/__tests__/route.test.ts`
- **Commit**: `test(api): POST /api/connect` then `feat(api): database connection endpoint`

---

### Step 11: API route — GET /api/schema
- **Test**: Mock connection manager. GET when connected returns schema JSON. GET when disconnected returns 404 with message.
- **Implement**: Create `apps/web/src/app/api/schema/route.ts` — gets active connection, runs introspection, returns schema.
- **Files**: `apps/web/src/app/api/schema/route.ts`, `apps/web/src/app/api/schema/__tests__/route.test.ts`
- **Commit**: `test(api): GET /api/schema` then `feat(api): schema introspection endpoint`

---

### Step 12: API route — POST /api/chat (SSE streaming)
- **Test**: Mock LM Studio client. POST with message + history streams SSE events. POST when LM Studio is down returns error event. POST without schema (no DB connected) sends prompt without schema context.
- **Implement**: Create `apps/web/src/app/api/chat/route.ts` — builds prompt, calls LM Studio, streams response as SSE.
- **Files**: `apps/web/src/app/api/chat/route.ts`, `apps/web/src/app/api/chat/__tests__/route.test.ts`
- **Commit**: `test(api): POST /api/chat streaming` then `feat(api): SSE streaming chat endpoint with LM Studio`

---

### Step 13: React context — ConnectionContext
- **Test**: Render `ConnectionProvider` — initial state is disconnected. Calling `connect(config)` updates status to connected. Calling `connect()` with error updates status with error message. Schema is populated after successful connection.
- **Implement**: Create `apps/web/src/contexts/connection-context.tsx` with provider, reducer, and `useConnection` hook. Calls `/api/connect` and `/api/schema`.
- **Files**: `apps/web/src/contexts/connection-context.tsx`, `apps/web/src/contexts/__tests__/connection-context.test.tsx`
- **Commit**: `test(ui): ConnectionContext provider` then `feat(ui): ConnectionContext with connect/disconnect and schema fetch`

---

### Step 14: React context — ChatContext
- **Test**: Render `ChatProvider` — initial messages are empty. `sendMessage('query')` adds user message, calls `/api/chat`, accumulates streamed tokens into assistant message. SQL is extracted from assistant response and set as `currentSql`. History trimmed at 20 messages.
- **Implement**: Create `apps/web/src/contexts/chat-context.tsx` with provider, reducer, `useChat` hook. Handles SSE parsing from `/api/chat`.
- **Files**: `apps/web/src/contexts/chat-context.tsx`, `apps/web/src/contexts/__tests__/chat-context.test.tsx`
- **Commit**: `test(ui): ChatContext provider` then `feat(ui): ChatContext with SSE streaming and SQL extraction`

---

### Step 15: Connection dialog component
- **Test**: Render `ConnectionDialog` — shows database type selector (PostgreSQL/SQLite). Selecting PostgreSQL shows host/port/database/username/password fields. Selecting SQLite shows file path field. Submit button is disabled when required fields are empty. On submit, `onConnect` callback is called with correct config.
- **Implement**: Create `apps/web/src/components/connection-dialog.tsx` using Catalyst Dialog, Select, Input, Button, Fieldset components.
- **Files**: `apps/web/src/components/connection-dialog.tsx`, `apps/web/src/components/__tests__/connection-dialog.test.tsx`
- **Commit**: `test(ui): connection dialog form` then `feat(ui): connection dialog with PG/SQLite forms`

---

### Step 16: Schema browser component
- **Test**: Render `SchemaBrowser` with schema data — all tables are listed. Clicking a table expands to show columns with types. FK columns show badge. Render with empty schema — shows "No tables found". Render with null schema — shows "Connect a database to browse schema".
- **Implement**: Create `apps/web/src/components/schema-browser.tsx` using Catalyst Sidebar components (SidebarSection, SidebarHeading, SidebarItem, SidebarLabel) + Headless Disclosure for expand/collapse.
- **Files**: `apps/web/src/components/schema-browser.tsx`, `apps/web/src/components/__tests__/schema-browser.test.tsx`
- **Commit**: `test(ui): schema browser rendering` then `feat(ui): schema browser with expandable tables and FK badges`

---

### Step 17: Chat panel component
- **Test**: Render `ChatPanel` with messages — user and assistant messages are displayed correctly. SQL blocks in assistant messages are rendered distinctly. Input field accepts text. Submit fires `onSend` with input value and clears field. Submit is disabled while loading. Empty input cannot be submitted.
- **Implement**: Create `apps/web/src/components/chat-panel.tsx` — message list, input form, loading indicator.
- **Files**: `apps/web/src/components/chat-panel.tsx`, `apps/web/src/components/__tests__/chat-panel.test.tsx`
- **Commit**: `test(ui): chat panel messages and input` then `feat(ui): chat panel with message list and input form`

---

### Step 18: Query editor component (CodeMirror)
- **Test**: Render `QueryEditor` with SQL string — content is displayed. Content is editable. Copy button calls `navigator.clipboard.writeText` with current content. Render with empty string — shows placeholder.
- **Implement**: Create `apps/web/src/components/query-editor.tsx` using CodeMirror with `@codemirror/lang-sql` for syntax highlighting + Catalyst Button for copy.
- **Files**: `apps/web/src/components/query-editor.tsx`, `apps/web/src/components/__tests__/query-editor.test.tsx`
- **Commit**: `test(ui): query editor with copy` then `feat(ui): CodeMirror SQL editor with copy-to-clipboard`

---

### Step 19: Header with connection status
- **Test**: Render `AppHeader` when disconnected — shows "Not connected". Render when connected to PostgreSQL — shows "Connected — PostgreSQL". Render when connected to SQLite — shows "Connected — SQLite". "Connect" button opens connection dialog.
- **Implement**: Create `apps/web/src/components/app-header.tsx` using Catalyst Navbar, Badge, Button.
- **Files**: `apps/web/src/components/app-header.tsx`, `apps/web/src/components/__tests__/app-header.test.tsx`
- **Commit**: `test(ui): header connection status` then `feat(ui): header with connection status badge and connect button`

---

### Step 20: Main page layout — wire everything together
- **Test**: Render main page — SidebarLayout is rendered with schema browser in sidebar, chat panel and query editor in main area. Connection dialog opens when connect button is clicked. Verify contexts are provided (ConnectionProvider, ChatProvider wrap the page).
- **Implement**: Update `apps/web/src/app/page.tsx` and `apps/web/src/app/layout.tsx` — compose all components with SidebarLayout. Wire contexts.
- **Files**: `apps/web/src/app/page.tsx`, `apps/web/src/app/layout.tsx`, `apps/web/src/app/providers.tsx`
- **Commit**: `test(ui): main page layout composition` then `feat(ui): main page with sidebar layout, chat, editor, and schema browser`

---

### Step 21: Integration test — end-to-end chat flow
- **Test**: Mock API routes. User opens app (disconnected) → types message → assistant response streams in → SQL extracted to editor → user sends follow-up → editor updates. Verify conversation history accumulates. Verify copy button works.
- **Implement**: Fix any issues found during integration testing. Ensure all components work together.
- **Files**: `apps/web/src/__tests__/integration/chat-flow.test.tsx`
- **Commit**: `test(integration): end-to-end chat flow`

---

### Step 22: Integration test — connection + schema flow
- **Test**: Mock API routes. User clicks Connect → fills PostgreSQL form → submits → header updates to "Connected — PostgreSQL" → schema browser shows tables → user expands table → columns visible. User connects to SQLite → previous connection replaced → schema browser updates.
- **Implement**: Fix any issues found during integration testing.
- **Files**: `apps/web/src/__tests__/integration/connection-flow.test.tsx`
- **Commit**: `test(integration): database connection and schema browsing flow`

---

## Status Checklist

- [x] Step 1: Monorepo scaffold
- [x] Step 2: Shared types and utilities
- [x] Step 3: PostgreSQL connection service
- [x] Step 4: SQLite connection service
- [x] Step 5: Unified connection manager
- [ ] Step 6: PostgreSQL schema introspection
- [x] Step 7: SQLite schema introspection
- [x] Step 8: LM Studio streaming client
- [x] Step 9: Prompt builder
- [x] Step 10: API — POST /api/connect
- [x] Step 11: API — GET /api/schema
- [x] Step 12: API — POST /api/chat (SSE)
- [x] Step 13: ConnectionContext
- [x] Step 14: ChatContext
- [x] Step 15: Connection dialog
- [ ] Step 16: Schema browser
- [ ] Step 17: Chat panel
- [ ] Step 18: Query editor (CodeMirror)
- [ ] Step 19: Header with connection status
- [ ] Step 20: Main page layout
- [ ] Step 21: Integration — chat flow
- [ ] Step 22: Integration — connection + schema flow
